import { CreatePlanStrategy } from "./Strategy";
import { NewPlan } from "./types";

class SpecificRoutinePlanStrategy implements CreatePlanStrategy {
    createPlan(newPlan: NewPlan): void {
        
    }
}